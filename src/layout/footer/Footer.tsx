import React from 'react';
import styled from "styled-components";

export const Footer = () => {
    return (
        <FooterContainer>

        </FooterContainer>
    );
};

const FooterContainer = styled.div`
height: 200px;
`
